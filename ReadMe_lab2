Приведенный проект (Demo) работает с набором изображений. 
Цель - найти необходимый контур, который выделяется на первом из ряда изображений, лежащих в однй директории.

Входные данные:  - координаты прямоугольника в пикселях, внутри которого находится контур для поиска (x1,y1,x2,y2). Например - (400,480),(1020,1020)
                 - путь до директории, в которой лежат обрабатываемые изображения
Выходные данные: - по каждому исходному изображению вычисляется процент совпадения с эталоном (заданным контуром), результат сохраняется
                   в файле "Accordance.txt" в папке проекта
                 - создается папка "new_rez", в которой по каждому исходному изображению формируются промежуточные для отслеживания работы 
                   и результат (для лабораторной работы роли не играют)

Папка с изображениями под названием "new" лежит в папке проекта 

